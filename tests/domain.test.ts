import {describe,expect,it} from "vitest";
import {calculateQuotePrice,requestSchema,resolvePlan} from "@/lib/domain";
import {jsonStringify,stableJson} from "@/lib/hash";
describe("pricing",()=>{it("keeps provider costs below the 60 percent ceiling",()=>{const quote=calculateQuotePrice(600000);expect(quote.customerPriceAtomic).toBe(1000000);expect(quote.operationalBudgetAtomic).toBe(600000);expect(quote.genlayerReserveAtomic).toBe(100000);expect(quote.revenueReserveAtomic).toBe(300000)});it("raises the customer price when provider cost exceeds the minimum allocation",()=>{const quote=calculateQuotePrice(750000);expect(quote.customerPriceAtomic).toBe(1250000);expect(quote.operationalBudgetAtomic).toBeGreaterThanOrEqual(750000)});it("serializes bigint values safely",()=>{expect(jsonStringify({amount:1n})).toBe('{"amount":"1"}');expect(stableJson({amount:1n})).toBe('{"amount":"1"}')});it("uses fixed plans only for timeout and execution policy",()=>{const input=requestSchema.parse({product:"decision",task:"Should this agent approve the vendor payment?",clientRequestId:"request-12345678"});expect(resolvePlan(input).maxProviders).toBe(2);expect(resolvePlan(input).timeoutMinutes).toBeGreaterThan(0)});it("rejects short tasks",()=>{expect(requestSchema.safeParse({product:"gateway",task:"short",clientRequestId:"request-12345678"}).success).toBe(false)})});
