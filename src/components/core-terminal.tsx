"use client";
import { useEffect, useRef } from "react";
import * as THREE from "three";
export function CoreTerminal(){const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!ref.current)return;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(42,1,.1,100);camera.position.z=4.3;const renderer=new THREE.WebGLRenderer({alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(360,360);ref.current.appendChild(renderer.domElement);const group=new THREE.Group();scene.add(group);const core=new THREE.Mesh(new THREE.OctahedronGeometry(.78,1),new THREE.MeshBasicMaterial({color:0x00a3ff,wireframe:false,transparent:true,opacity:.92}));group.add(core);const shell=new THREE.Mesh(new THREE.IcosahedronGeometry(1.28,2),new THREE.MeshBasicMaterial({color:0x7ecbff,wireframe:true,transparent:true,opacity:.38}));group.add(shell);for(const rotation of [[0,0,0],[Math.PI/3,.2,.4],[-Math.PI/4,.7,.2]]){const ring=new THREE.Mesh(new THREE.TorusGeometry(1.62,.012,8,120),new THREE.MeshBasicMaterial({color:0x00a3ff,transparent:true,opacity:.6}));ring.rotation.set(...rotation as [number,number,number]);group.add(ring)}const light=new THREE.PointLight(0x00a3ff,16,7);scene.add(light);let frame=0;const animate=()=>{frame=requestAnimationFrame(animate);const t=performance.now()/1000;group.rotation.y+=.008;group.rotation.z+=.003;core.scale.setScalar(1+Math.sin(t*4)*.08);shell.rotation.x-=.004;group.position.x+=(.08-(group.position.x))*.03;renderer.render(scene,camera)};animate();const move=(event:MouseEvent)=>{const x=event.clientX/innerWidth-.5;group.rotation.x=x*.3;group.rotation.y+=x*.02};window.addEventListener("mousemove",move);return()=>{cancelAnimationFrame(frame);window.removeEventListener("mousemove",move);renderer.dispose();ref.current?.removeChild(renderer.domElement)}},[]);return <div ref={ref} className="core-terminal" aria-label="Animated Gen-X402 processing core"/>}
