export default function Page(){return <main className="section"><span className="badge">Coming in account release</span><h1 style={{fontSize:52}}>status</h1><div className="card"><p className="muted">This view is intentionally disabled until wallet authentication and encrypted account indexes are added. Paid API jobs already work without accounts through signed x402 payments.</p></div></main>}
