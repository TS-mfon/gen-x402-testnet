export default function Page() {
  return <main className="section docs-page">
    <div className="phase-line"><span>PROTOCOL REFERENCE</span><i />API V1 / SERVERLESS</div>
    <div className="workbench-head"><div><span className="eyebrow">AGENT INTEGRATION</span><h1>Access the intelligence layer.</h1><p>Call a paid endpoint with an x402-compatible client. The first request returns a payment challenge; the client authorizes USDC and retries with proof of payment.</p></div><div className="module-readout"><span>NETWORK</span><b>BASE SEPOLIA</b><span>MODE</span><b>VALIDATION</b><span>FACILITATOR</span><b>COINBASE CDP</b></div></div>
    <div className="formLayout"><section className="card"><h2>Endpoints</h2><pre>{`POST /api/v1/gateway\nPOST /api/v1/investigate\nPOST /api/v1/procure\nPOST /api/v1/decide\nPOST /api/v1/quality-check\nGET  /api/v1/jobs/:id`}</pre><h2>Request</h2><pre>{JSON.stringify({task:"Should my agent pay this invoice?",subject:{address:"0x..."},context:{amount:"2000 USDC"},riskLevel:"high",clientRequestId:"client-generated-unique-id"},null,2)}</pre></section>
    <aside className="card"><h2>Operational Guarantees</h2><ul className="muted"><li>Base Sepolia is the only payment network in this validation deployment.</li><li>The browser wallet signs payments; the platform never receives the user private key.</li><li>CDP API credentials authenticate the server-side facilitator and managed operations wallet.</li><li>Jobs persist in private Vercel Blob objects, not a database.</li><li>Final product verdicts come from the finalized GenLayer Studionet contract result.</li><li>The platform returns analysis and decisions; it never executes customer transactions.</li><li>Upstream x402 purchasing activates only for explicitly approved real provider endpoints.</li></ul></aside></div>
  </main>;
}
