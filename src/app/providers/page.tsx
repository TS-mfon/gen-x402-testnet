import {listProviders} from "@/lib/store";export const dynamic="force-dynamic";export default async function Page(){const providers=await listProviders() as any[];return <main className="section"><span className="badge">CDP discovery · Base Sepolia</span><h1 style={{fontSize:52}}>Provider Registry</h1><p className="muted">All discovered resources are visible. Only providers with an explicit safe fixture and Base Sepolia USDC challenge are charged by automated tests.</p><div className="card"><table className="table"><thead><tr><th>Provider</th><th>Method</th><th>Category</th><th>Status</th><th>Price</th><th>Test</th></tr></thead><tbody>{providers.map((p:any)=><tr key={p.id}><td><b>{p.name}</b><div className="muted">{p.description}</div></td><td>{p.method}</td><td>{p.category}</td><td>{p.approval_status}</td><td>{Number(p.price_atomic)/1e6} USDC</td><td>{p.test_enabled?"ENABLED":"DISCOVERY ONLY"}</td></tr>)}</tbody></table></div></main>}
